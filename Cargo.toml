[package]
name = "ai-code-constructor"
version = "0.1.0"
edition = "2024"

[dependencies]
httpdate = "1.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
ureq = { version = "2.12", default-features = false, features = ["tls"] }
